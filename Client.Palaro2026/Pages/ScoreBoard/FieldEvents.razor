@page "/scoreboard/field-events"
@using Client.Palaro2026.Services
@using System.Linq
@layout NoMainLayout
@inject APIService apiService
@inject NavigationManager Navigation
@implements IDisposable

<PageTitle>Field Events Scoreboard | PALARO 2026</PageTitle>

<style>
    /* --- IMPORTS --- */
    @@import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@500;700;900&family=Rajdhani:wght@500;600;700&display=swap');

    :root {
        --bg-dark: #0a0a0a;
        --card-bg: rgba(30, 30, 30, 0.7);
        --card-border: rgba(255, 255, 255, 0.1);
        --accent-gold: #FFD700;
        --accent-blue: #00e5ff;
        --text-white: #ffffff;
    }

    body {
        background-color: var(--bg-dark);
        background-image: radial-gradient(circle at 15% 50%, rgba(0, 229, 255, 0.05) 0%, transparent 25%), radial-gradient(circle at 85% 30%, rgba(255, 215, 0, 0.05) 0%, transparent 25%);
        font-family: 'Rajdhani', sans-serif;
        color: white;
        min-height: 100vh;
    }

    .scoreboard-header {
        text-align: center;
        padding: 40px 0 20px;
    }

    .main-title {
        font-family: 'Orbitron', sans-serif;
        font-weight: 900;
        font-size: 3rem;
        background: linear-gradient(180deg, #FFFFFF 0%, #b0b0b0 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        letter-spacing: 4px;
    }

    /* MODERN CARD */
    .modern-card {
        background: var(--card-bg);
        border: 1px solid var(--card-border);
        border-radius: 16px;
        backdrop-filter: blur(10px);
        display: flex;
        flex-direction: column;
        height: 500px; /* Fixed height para uniform */
        overflow: hidden;
        transition: border-color 0.3s ease, box-shadow 0.3s ease;
    }

        .modern-card:hover {
            border-color: var(--accent-gold);
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
        }

    .card-header {
        padding: 15px 20px;
        background: rgba(0,0,0,0.2);
        border-bottom: 1px solid rgba(255,255,255,0.05);
        flex-shrink: 0;
    }

    .event-name {
        font-family: 'Orbitron', sans-serif;
        font-size: 1.2rem;
        font-weight: 700;
        color: var(--accent-gold);
    }

    .event-details {
        font-size: 0.9rem;
        color: #aaa;
    }

    /* SCROLLABLE PLAYER LIST */
    .player-list-container {
        flex-grow: 1;
        overflow-y: auto;
        padding: 10px;
    }

        /* Custom Scrollbar */
        .player-list-container::-webkit-scrollbar {
            width: 6px;
        }

        .player-list-container::-webkit-scrollbar-track {
            background: rgba(0,0,0,0.1);
        }

        .player-list-container::-webkit-scrollbar-thumb {
            background: var(--accent-gold);
            border-radius: 3px;
        }

    .player-row {
        display: flex;
        align-items: center;
        padding: 8px 10px;
        border-bottom: 1px solid rgba(255,255,255,0.05);
        font-size: 0.95rem;
    }

        .player-row:last-child {
            border-bottom: none;
        }

    .player-rank {
        width: 30px;
        font-family: 'Orbitron', sans-serif;
        font-weight: bold;
        color: #888;
    }

    .rank-1 {
        color: #FFD700;
        text-shadow: 0 0 10px rgba(255, 215, 0, 0.5);
        font-size: 1.1rem;
    }

    .rank-2 {
        color: #C0C0C0;
    }

    .rank-3 {
        color: #CD7F32;
    }

    .player-info {
        flex-grow: 1;
        overflow: hidden;
        white-space: nowrap;
        text-overflow: ellipsis;
        padding-right: 10px;
    }

    .player-name {
        font-weight: 700;
        display: block;
    }

    .player-region {
        font-size: 0.75rem;
        color: var(--accent-blue);
    }

    .player-score {
        font-family: 'Orbitron', sans-serif;
        font-weight: bold;
        text-align: right;
        min-width: 60px;
    }

    /* FOOTER BUTTON */
    .card-footer {
        padding: 15px;
        background: rgba(0,0,0,0.3);
        border-top: 1px solid rgba(255,255,255,0.05);
        flex-shrink: 0;
    }

    .tv-btn {
        display: block;
        width: 100%;
        padding: 10px;
        background: rgba(255, 255, 255, 0.05);
        color: white;
        text-align: center;
        text-decoration: none;
        border-radius: 8px;
        font-family: 'Orbitron';
        font-size: 0.8rem;
        font-weight: bold;
        letter-spacing: 1px;
        transition: all 0.2s;
        border: 1px solid rgba(255, 255, 255, 0.1);
    }

        .tv-btn:hover {
            background: var(--accent-gold);
            color: black;
            box-shadow: 0 0 15px rgba(255, 215, 0, 0.4);
        }

    .badge-live {
        background: #ff1744;
        color: white;
        padding: 2px 8px;
        border-radius: 4px;
        font-size: 0.7rem;
        font-weight: bold;
        float: right;
        animation: pulse 1.5s infinite;
    }

    .badge-final {
        background: #4CAF50;
        color: white;
        padding: 2px 8px;
        border-radius: 4px;
        font-size: 0.7rem;
        font-weight: bold;
        float: right;
    }

    @@keyframes pulse {
        0% {
            opacity: 1;
        }

        50% {
            opacity: 0.6;
        }

        100% {
            opacity: 1;
        }
    }
</style>

<MudContainer MaxWidth="MaxWidth.ExtraLarge" Class="pb-16">

    <div class="scoreboard-header">
        <div class="main-title">FIELD EVENTS</div>
        <MudText Typo="Typo.h6" Style="color: #888;">OFFICIAL STANDINGS</MudText>
    </div>

    @if (_isLoading)
    {
        <MudStack AlignItems="AlignItems.Center" Class="mt-16">
            <MudProgressCircular Indeterminate="true" Color="Color.Warning" Size="Size.Large" />
            <MudText Class="mt-4">Loading Data...</MudText>
        </MudStack>
    }
    else
    {
        <MudTabs Elevation="0" Rounded="true" ApplyEffectsToContainer="true" Centered="true" Color="Color.Dark" Class="mt-4">

            <!-- LIVE TAB -->
            <MudTabPanel Text="ONGOING" Icon="@Icons.Material.Filled.Stream">
                <MudGrid Class="mt-4" Spacing="3">
                    @if (!_ongoingEvents.Any())
                    {
                        <MudItem xs="12"><MudText Align="Align.Center">No ongoing events.</MudText></MudItem>
                    }
                    else
                    {
                        @foreach (var evt in _ongoingEvents)
                        {
                            <MudItem xs="12" sm="6" md="4" lg="3">
                                <div class="modern-card">
                                    <!-- HEADER -->
                                    <div class="card-header">
                                        <span class="badge-live">LIVE</span>
                                        <div class="event-name">@evt.Subcategory</div>
                                        <div class="event-details">@evt.Gender • @evt.Level</div>
                                        <div class="event-details" style="font-size: 0.8rem; color: var(--accent-blue)">@evt.EventStage</div>
                                    </div>

                                    <!-- LIST OF PLAYERS -->
                                    <div class="player-list-container">
                                        @if (_eventRankings.ContainsKey(evt.ID) && _eventRankings[evt.ID].Any())
                                        {
                                            @foreach (var player in _eventRankings[evt.ID])
                                            {
                                                <div class="player-row">
                                                    <div class="player-rank @GetRankClass(player.Rank)">
                                                        @(player.Rank > 0 ? $"#{player.Rank}" : "-")
                                                    </div>
                                                    <div class="player-info">
                                                        <span class="player-name">@player.PlayerName</span>
                                                        <span class="player-region">@player.RegionAbbreviation</span>
                                                    </div>
                                                    <div class="player-score">
                                                        @player.BestResult.ToString("0.00")
                                                    </div>
                                                </div>
                                            }
                                        }
                                        else
                                        {
                                            <div class="d-flex justify-center align-center h-100">
                                                <MudText Typo="Typo.caption" Style="color:#666">Waiting for participants...</MudText>
                                            </div>
                                        }
                                    </div>

                                    <!-- FOOTER LINK -->
                                    <div class="card-footer">
                                        <a href="/scoreboard/field-events/@evt.ID" target="_blank" class="tv-btn">
                                            OPEN TV VIEW <MudIcon Icon="@Icons.Material.Filled.OpenInNew" Size="Size.Small" Class="ml-2" />
                                        </a>
                                    </div>
                                </div>
                            </MudItem>
                        }
                    }
                </MudGrid>
            </MudTabPanel>

            <!-- FINISHED TAB -->
            <MudTabPanel Text="FINISHED" Icon="@Icons.Material.Filled.EmojiEvents">
                <MudGrid Class="mt-4" Spacing="3">
                    @foreach (var evt in _finishedEvents)
                    {
                        <MudItem xs="12" sm="6" md="4" lg="3">
                            <div class="modern-card">
                                <div class="card-header">
                                    <span class="badge-final">FINAL</span>
                                    <div class="event-name">@evt.Subcategory</div>
                                    <div class="event-details">@evt.Gender • @evt.Level</div>
                                    <div class="event-details" style="font-size: 0.8rem; color: var(--accent-blue)">@evt.EventStage</div>
                                </div>

                                <div class="player-list-container">
                                    @if (_eventRankings.ContainsKey(evt.ID))
                                    {
                                        @foreach (var player in _eventRankings[evt.ID])
                                        {
                                            <div class="player-row">
                                                <div class="player-rank @GetRankClass(player.Rank)">
                                                    #@player.Rank
                                                </div>
                                                <div class="player-info">
                                                    <span class="player-name">@player.PlayerName</span>
                                                    <span class="player-region">@player.RegionAbbreviation</span>
                                                </div>
                                                <div class="player-score">
                                                    @player.BestResult.ToString("0.00")
                                                </div>
                                            </div>
                                        }
                                    }
                                </div>

                                <div class="card-footer">
                                    <a href="/scoreboard/field-events/@evt.ID" target="_blank" class="tv-btn">
                                        VIEW OFFICIAL RESULTS
                                    </a>
                                </div>
                            </div>
                        </MudItem>
                    }
                </MudGrid>
            </MudTabPanel>

        </MudTabs>
    }

</MudContainer>

@code {
    // --- VARIABLES ---
    private List<EventInfo> _allEvents = new();
    private List<EventInfo> _ongoingEvents = new();
    private List<EventInfo> _finishedEvents = new();

    // List<RankingDTO> ang value ani para sa scrollable list
    private Dictionary<string, List<RankingDTO>> _eventRankings = new();

    private bool _isLoading = true;
    private System.Threading.Timer? _timer;

    // --- LIFECYCLE ---
    protected override async Task OnInitializedAsync()
    {
        await LoadData();
        // Auto refresh every 10 seconds
        _timer = new System.Threading.Timer(async _ => await InvokeAsync(LoadData), null, 10000, 10000);
    }

    private async Task LoadData()
    {
        try
        {
            // FIX HERE: Tangtangon ang List<> sa Generic Type.
            // Ang imong APIService murag 'GetAsync<T>' returns 'List<T>'.
            // So <EventInfo> ra ang ibutang, dili <List<EventInfo>>.
            var events = await apiService.GetAsync<EventInfo>("FieldEvents/Sports/AthleticsFieldEvents");

            if (events != null)
            {
                // GroupBy ID ug kuhaon ang first entry para walay duplicates
                _allEvents = events
                    .GroupBy(e => e.ID)
                    .Select(g => g.First())
                    .ToList();

                _ongoingEvents = _allEvents.Where(e => e.EventStage != "Completed").ToList();
                _finishedEvents = _allEvents.Where(e => e.EventStage == "Completed").ToList();

                // 2. Get Rankings per Event
                foreach (var evt in _allEvents)
                {
                    // FIX HERE: Tangtangon sad ang List<> diri. <RankingDTO> ra.
                    var rankings = await apiService.GetAsync<RankingDTO>($"FieldEvents/GetRankings/{evt.ID}");

                    if (rankings != null && rankings.Any())
                    {
                        // Save all rankings sorted by Rank
                        _eventRankings[evt.ID] = rankings.OrderBy(r => r.Rank).ToList();
                    }
                }
            }
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error: {ex.Message}");
        }
        finally
        {
            _isLoading = false;
            StateHasChanged();
        }
    }

    // --- HELPER FOR RANK COLORS ---
    private string GetRankClass(int rank)
    {
        return rank switch
        {
            1 => "rank-1",
            2 => "rank-2",
            3 => "rank-3",
            _ => ""
        };
    }

    public void Dispose()
    {
        _timer?.Dispose();
    }

    // --- MODELS ---
    public class EventInfo
    {
        public string ID { get; set; } = "";
        public string? Sport { get; set; }
        public string? Subcategory { get; set; }
        public string? Gender { get; set; }
        public string? Level { get; set; }
        public string? EventStage { get; set; }
    }

    public class RankingDTO
    {
        public int RegionID { get; set; }
        public string? PlayerID { get; set; }
        public decimal BestResult { get; set; }
        public string RegionAbbreviation { get; set; } = "";
        public string? PlayerName { get; set; }
        public int Rank { get; set; }
    }
}